# object
# Retorna um novo objeto.
# object é a base de todas as classes.
# Ele tem seus métodos que são comuns para todas as instâncias das classes.
# Essa função não aceita argumentos.

A = object
print(A)
print(object)
