# bytearray([source[, encoding[, errors]]])
# Retorna um novo array de bytes.
# O parâmetro source é opcional, e pode ser utilizado para inicializar o array
# em várias maneiras diferentes.

A = bytearray(10)
print(A)
for i in A:
    print(i, end=" ")
