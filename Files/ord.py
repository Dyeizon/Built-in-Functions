# ord(c)
# Dada uma String representado um caractere Unicode, retorna um inteiro representando o código Unicode desse caractere
# Por exemplo, ord('a') retorna o inteiro 97, e ord('€') retorna 8364.
# Essa função é o inverso de chr()

print(ord('♦'))

print(str(chr(1282)) + " = " + str(ord("Ԃ")))
